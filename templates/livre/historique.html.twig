{% extends 'base.html.twig' %}

{% block title %}📖 Historique des réservations{% endblock %}

{% block body %}
<div class="bento-wrapper">
    <h1 class="bento-title">📖 Historique des réservations</h1>

    <div class="bento-table">
        <table>
            <thead>
                <tr>
                    <th>Utilisateur</th>
                    <th>Livre</th>
                    <th>Date</th>
                </tr>
            </thead>
            <tbody>
                {% for res in reservations %}
                    <tr>
                        <td>{{ res.utilisateur.nom }} {{ res.utilisateur.prenom }}</td>
                        <td>{{ res.livre.titre }}</td>
                        <td>{{ res.dateReservation|date('d/m/Y à H:i') }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">Aucune réservation trouvée.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
