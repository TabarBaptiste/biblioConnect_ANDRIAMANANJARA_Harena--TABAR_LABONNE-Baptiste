{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}" class="btn-return">← Retour au catalogue</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="bento-btn">Mettre à jour</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
